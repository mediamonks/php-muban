<{{ component.as }}
data-component="{{ component.component }}"
class="{{ component.style }} {{ component.className|join(' ') }}"
{% if component.ariaLabel %} aria-label="{{ component.ariaLabel }}"{% endif %}
{% if component.id %} id="{{ component.id }}"{% endif %}
>
{{ component.copy }}
</{{ component.as }}>