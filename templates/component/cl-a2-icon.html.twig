{# @var component \MediaMonks\Muban\Component\Library\CLA2Icon #}

<span data-component="{{ component.component }}" data-name="{{ component.name }}" class="{{ component.className|join(' ') }}"{% if component.id %} id="{{ component.id }}"{% endif %}/>